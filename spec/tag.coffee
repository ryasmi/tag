tag = require '../src/tag'

# Describes tag.
describe('tag', ->
  # returns a function to create elements.
    # opts - class, on, tag-show
    # content - elem, text, elem
    # returns an element with content.
      # elem
      # text
    # returns an element with options.
      # tag-show
      # on array/func
      # class
    # returns an element with options and content.
)
